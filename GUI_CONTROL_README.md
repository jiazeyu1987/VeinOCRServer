# OCR 和图像对比控制面板使用说明

## 功能介绍

新增的 GUI 控制面板允许您通过简单的点击按钮同时控制 OCR 识别和图像对比功能。

## 使用方法

### 启动 GUI 控制面板

```bash
python treat_compare_img.py --gui
```

### GUI 界面说明

1. **状态显示**：显示当前运行状态（已停止/运行中）
2. **Point ID**：输入点 ID（默认 123）
3. **超时时间**：设置图片对比的超时时间，单位秒（默认 10 秒）
4. **启动按钮**：点击后同时启动 OCR 识别和图片对比
5. **停止按钮**：点击后停止 OCR 识别和图片对比

### 运行原来的测试代码

如果不使用 `--gui` 参数，将运行原来的测试代码：

```bash
python treat_compare_img.py
```

## 功能特点

### 同时启动
- 点击"启动 OCR + 图像对比"按钮后：
  - OCR 线程：持续进行屏幕截图和 OCR 识别
  - 图片对比线程：进行治疗前后的图像对比检测

### 同时停止
- 点击"停止"按钮后：
  - OCR 识别停止
  - 图片对比停止
  - 所有线程安全退出

### 线程安全
- 使用 `threading.Event()` 进行线程间通信
- daemon 线程确保 GUI 关闭时自动清理
- 异常处理防止线程崩溃

## 日志记录

所有操作都会记录到日志文件中：
- 路径：`ocrlog/ocrapp_YYYY-MM-DD.log`
- 包含启动、停止、错误等信息

## 注意事项

1. **配置文件**：程序会读取 `settings` 文件中的配置（如果存在）
2. **图片保存**：图片对比结果会保存到 `D:/software_data/imgs/` 目录
3. **数据库**：如果 `is_save=True`，对比结果会存入数据库
4. **GPU 支持**：根据配置文件自动选择使用 CPU 或 GPU

## 兼容性

- 保留原有的 `ComparePoints` 类不变
- 不影响 `server.py` 的使用
- 可以作为独立的控制程序运行

## 示例工作流程

1. 启动程序：`python treat_compare_img.py --gui`
2. 设置 Point ID（例如：456）
3. 设置超时时间（例如：20 秒）
4. 点击"启动 OCR + 图像对比"
5. 观察状态变为"运行中"（绿色）
6. 进行需要记录的操作
7. 点击"停止"按钮
8. 观察状态变为"已停止"
9. 查看日志和结果图片

## 常见问题

### Q: 点击启动后没有反应？
A: 检查日志文件，查看是否有错误信息。

### Q: 如何同时运行多个对比任务？
A: 每次启动新的任务前，需要先停止当前任务。

### Q: 能否在没有配置文件的情况下运行？
A: 可以，程序会使用默认配置。

## 技术实现

- **GUI 框架**：tkinter（Python 内置）
- **线程管理**：threading 模块
- **日志记录**：logging 模块
- **OCR 引擎**：PaddleOCR
- **图像处理**：OpenCV
